# Port exposé sur ton Synology (accessible via http://synology-ip:PORT)
PORT=3000

# Clé de chiffrement AES-256 pour les clés API Binance (32 caractères min)
# Génère-en une avec : openssl rand -hex 32
ENCRYPTION_SECRET=change_this_to_a_random_64_char_hex_string
